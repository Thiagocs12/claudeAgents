## 20260915130215-clonar-cedente-completo-prod-hml
Status: respondida
Status-historico-7: respondida
Status-historico-1: respondida
Id-original-da-duvida-1: mc-rat-rating-indicador-fora-do-padrao-mc-cad
Pergunta-1: Mapeando a fase POC (proposta) da tarefa `20260915130215-clonar-cedente-completo-prod-hml`, encontrei que `MC_POC_RATING_INDICADOR_RESULTADO.idRatingIndicador` (coluna NOT NULL) e `.idRatingIndicadorItem` (nullable) apontam para `MC_RAT_RATING_INDICADOR` e `MC_RAT_RATING_INDICADOR_ITEM` — duas tabelas com cara de catálogo/domínio compartilhado (indicador e item de indicador de rating, não algo que "pertence" a uma proposta específica), mas com prefixo `MC_RAT_`, fora do padrão `MC_CAD_*` que a automação já sabe resolver automaticamente como catálogo (busca por chave natural em HML, cria se faltar). Nenhuma das duas tabelas foi citada na tarefa original (nem na lista explícita, nem na nota sobre "Dependências de catálogo (MC_CAD_* genéricas)"). Preciso de uma decisão: (1) tratar `MC_RAT_RATING_INDICADOR`/`_ITEM` como catálogo, resolvidas pelo mesmo padrão das `MC_CAD_*` (mesma lógica de dependencias.js/estoque.js, só que buscando nessas duas tabelas em vez de uma `MC_CAD_*`)? Ou (2) outro tratamento? Sem resposta, `MC_POC_RATING_INDICADOR_RESULTADO` não pode ser inserida em HML (a coluna `idRatingIndicador` é NOT NULL) — o resto da fase POC já está mapeado e commitado (branch `cedente/clonar-cedente-completo-prod-hml`, ainda local).
Resposta-1: Thiago confirmou opção 1 (2026-09-15): tratar `MC_RAT_RATING_INDICADOR` e `MC_RAT_RATING_INDICADOR_ITEM` como catálogo, resolvidas pelo mesmo padrão de dependência já usado nas `MC_CAD_*` (busca por chave natural em HML, cria se faltar). [Já implementado e commitado — commit `5edaad0` na branch da tarefa.]

Status-historico-2: respondida
Id-original-da-duvida-2: conexao-sql-server-prod-hml-inacessivel-20260915
Pergunta-2: Ao retomar esta tarefa para mapear a fase `comitê` (script `investigar-schema-comite.cjs`, já preparado em `repo/`, mesmo padrão de investigação real usado nas fases anteriores), a conexão SQL Server falhou. Não é dúvida de escopo/negócio — é bloqueio de infraestrutura: um teste TCP puro (sem passar pelo driver/autenticação) contra `PROD_DB_HOST:PROD_DB_PORT` **e** `HOMOLOG_DB_HOST:HOMOLOG_DB_PORT` (lidos de `repo/.env`) deu timeout (~8s) nos dois — ou seja, não é algo específico de PROD nem de credencial, parece a máquina sem rota de rede para o SQL Server neste momento (VPN desconectada? Firewall?). Nas duas fases anteriores (prospect, POC) a mesma investigação funcionou normalmente. Preciso que confirme se a VPN/rede de acesso ao SQL Server está ativa nesta máquina — sem isso não dá pra prosseguir com o mapeamento da fase `comitê` (nem rodar `npm run test:safety`/lint muda isso, já que não dependem de rede; mas qualquer novo mapeamento real depende dessa conexão). Resumo: conexão com SQL Server (PROD e HML) inacessível via TCP nesta máquina, tarefa não pode avançar até confirmar rede/VPN.
Resposta-2: Thiago confirmou (2026-09-15): era a VPN, já reconectada. Pode tentar de novo a conexão com o SQL Server (PROD e HML) e seguir o mapeamento da fase comitê.

Status-historico-3: respondida
Id-original-da-duvida-3: conexao-sql-server-ainda-inacessivel-apos-vpn-20260915
Pergunta-3: Ao retomar o teste de conectividade (mesmo teste TCP puro, `net.createConnection`, script `.cjs` temporário dentro de `repo/`, removido depois) após a confirmação de que a VPN foi reconectada (Resposta-2 acima), a conexão continua em timeout (~5s) tanto para `PROD_DB_HOST:PROD_DB_PORT` (`10.101.1.18:1433`) quanto para `HOMOLOG_DB_HOST:HOMOLOG_DB_PORT` (`10.201.1.6:1433`) — testei duas vezes seguidas para descartar instabilidade pontual, mesmo resultado nas duas. Resumo: mesmo após a reconexão de VPN confirmada, o SQL Server (PROD e HML) continua inacessível via TCP nesta máquina — preciso que confirme novamente se a VPN está de fato ativa agora (pode ter caído de novo, ou a reconexão anterior não cobriu a rota até esses hosts) antes de eu tentar de novo o mapeamento da fase `comitê`.
Resposta-3: Thiago confirmou (2026-09-15): era a VPN de novo, já reconectada. Pode tentar a conexão de novo e continuar o mapeamento da fase comitê.

Status-historico-4: respondida
Id-original-da-duvida-4: id-participante-sem-fk-fisica-votacao-comite-20260915
Resumo: como resolver `idParticipante` (NOT NULL, sem FK física) em `MC_POC_COMITE_VOTACAO` e `MC_PORTAL_COMITE_VOTACAO`?
Pergunta-4: Com a conexão restabelecida, mapeei o grafo de FK real das 18 tabelas da fase `comitê` (commit `e635eab`, branch `cedente/clonar-cedente-completo-prod-hml`, ainda local) — quase tudo resolvido por catálogo (`MC_CAD_*`) ou por dependência estrutural, exceto uma coluna: `idParticipante` (NOT NULL) existe em `MC_POC_COMITE_VOTACAO` e `MC_PORTAL_COMITE_VOTACAO` (e também em `MC_CED_ATA_VOTACAO`, fase `cedente`, ainda não mapeada) **sem nenhuma constraint de FK física** (não aparece em `sys.foreign_keys`) e não existe nenhuma tabela chamada `PARTICIPANTE`/similar no schema. Investiguei uma amostra de `MC_POC_COMITE_VOTACAO`: os valores de `idParticipante` (ex.: 39, 40, 41, 45, 49) caem dentro do range de `MC_CAD_ANALISTA` (id 1 a 86, 84 linhas) — bate por coincidência de faixa, mas não é uma FK verificável, só uma hipótese minha. Preciso de uma decisão, já que a coluna é NOT NULL (diferente de outras colunas sem FK física já registradas nesta tarefa, que são todas nullable e ficaram deliberadamente sem resolução): (1) `idParticipante` de fato referencia `MC_CAD_ANALISTA.id` e devo resolvê-lo pelo mesmo padrão de catálogo (busca por chave natural em HML, cria se faltar) mesmo sem FK física declarada — igual ao precedente já aberto para `MC_RAT_RATING_INDICADOR`? Ou (2) referencia outra coisa (ex.: id de usuário/participante do Keycloak, não uma tabela do SQL Server, e nesse caso talvez nem precise de tradução PROD->HML por ser um id compartilhado)? Ou (3) outro tratamento? Sem resposta, `MC_POC_COMITE_VOTACAO` e `MC_PORTAL_COMITE_VOTACAO` não podem ser inseridas em HML — o resto da fase comitê já está mapeado e commitado.
Resposta-4: Thiago respondeu (2026-09-15): `idParticipante` são os votantes do comitê em PROD — **não copiar/traduzir os votantes reais de PROD**. Em vez disso, usar o próprio Thiago como participante em todas as linhas de votação clonadas (`MC_POC_COMITE_VOTACAO`, `MC_PORTAL_COMITE_VOTACAO`, e `MC_CED_ATA_VOTACAO` quando chegar na fase `cedente`) — ele confirma que deve existir um registro dele em `MC_CAD_ANALISTA` (ou tabela equivalente) em HML; localizar esse registro por nome (mesmo padrão de busca por chave natural já usado nas dependências de catálogo) e usar o `id` dele como `idParticipante`, em vez de resolver o valor original de PROD. Além disso: **todos os comitês clonados devem ficar marcados como votados e aprovados** (por ele) — ou seja, os campos de status/resultado de votação e de decisão do comitê devem refletir "aprovado", não o valor original vindo de PROD. Se ao mapear as colunas reais de status/resultado houver ambiguidade sobre qual campo exatamente representa "votado"/"aprovado", registrar nova dúvida específica com os nomes de coluna encontrados em vez de adivinhar. [Parte do idParticipante já implementada e commitada — commit `ff6c57c` na branch da tarefa, tipo de dependência novo `participante-fixo` em `mapeamentoCedente.js`.]

Id-original-da-duvida-5: campo-votado-aprovado-ambiguo-comite-votacao-20260915
Resumo: qual campo exatamente marca um comitê clonado como "votado e aprovado" — `resultadoVotacao` nunca é usado na prática em PROD?
Pergunta-5: Investiguei em PROD (`INFORMATION_SCHEMA.COLUMNS` + distribuição real de valores, script `.cjs` temporário em `repo/`, removido antes do commit) os campos de status/resultado de votação do comitê, pra implementar a segunda parte da Resposta-4 ("todos os comitês clonados devem ficar marcados como votados e aprovados"). Encontrei: `MC_POC_COMITE.situacaoVotacao` (NOT NULL, valores reais: `FINALIZADA` 5569, `NAO_INICIADA` 820, `INICIADA` 11, `REABERTA` 8) — parece claramente o campo de "votado" (`FINALIZADA`). Mas `MC_POC_COMITE.resultadoVotacao` (nullable, seria o campo óbvio de "aprovado/reprovado") está **null nas 6408 linhas da amostra, sem exceção** — ou seja, existe no schema mas não é usado na prática, então não há um valor real de "aprovado" observável ali pra eu replicar. Nas tabelas de voto individual, `MC_POC_COMITE_VOTACAO`/`MC_PORTAL_COMITE_VOTACAO` têm `situacaoVoto` (`CONCLUIDO`/`PENDENTE`/`AUSENTE`, e uma variante rara `FAVORAVEL` em 5 linhas de `MC_PORTAL_COMITE_VOTACAO`, parece dado inconsistente/legado) e `voto` (nullable, só valor não-nulo observado é `FAVORAVEL`). Não sei qual combinação exata representa "aprovado" pro propósito desta tarefa: (1) só `MC_POC_COMITE.situacaoVotacao = 'FINALIZADA'` (visto que `resultadoVotacao` não é usado, talvez não haja "resultado" formal a marcar, só o comitê votado)? (2) `situacaoVotacao = 'FINALIZADA'` + preencher `resultadoVotacao` com algum valor que hoje nunca é usado (qual valor exato, já que não há precedente em PROD)? (3) cada linha de `MC_POC_COMITE_VOTACAO`/`MC_PORTAL_COMITE_VOTACAO` com `situacaoVoto = 'CONCLUIDO'` e `voto = 'FAVORAVEL'` (reflete o voto individual do Thiago como aprovado)? (4) o "aprovado" na verdade é representado em `MC_POC_PROPOSTA.idSituacao` (catálogo `MC_CAD_SITUACAO`, já mapeado como dependência de catálogo comum) e não nos campos de comitê/votação em si — ainda não investiguei os valores desse catálogo. Alguma combinação de (1)+(3) e opcionalmente (4)? Sem essa decisão, não implemento a parte de "marcar como votado e aprovado" — o resto da fase comitê (grafo de FK, incluindo a resolução de `idParticipante`) já está mapeado/commitado e não é afetado por esta dúvida.
Resposta-5: Thiago escolheu a opção recomendada (2026-09-15): `MC_POC_COMITE.situacaoVotacao = 'FINALIZADA'` (comitê votado) + cada linha de voto do Thiago (as linhas com `idParticipante` = ele, ver Resposta-4) com `situacaoVoto = 'CONCLUIDO'` e `voto = 'FAVORAVEL'`. Não preencher `resultadoVotacao` — deixar `null`, já que não há precedente de uso real em PROD e não deve inventar um valor sem base. `MC_POC_PROPOSTA.idSituacao` (opção 4) fica fora desta decisão — continua sendo resolvido como dependência de catálogo comum, sem tratamento especial de "aprovado" a partir daqui (se isso gerar inconsistência prática mais adiante, registrar nova dúvida específica). [Já implementado e commitado — commit `9de9cf0` na branch da tarefa: novo campo declarativo `valoresFixos` em `mapeamentoCedente.js` + função pura `aplicarValoresFixos` em `clonagemCedente.js`.]

Status-historico-5: respondida
Id-original-da-duvida-6: conexao-sql-server-inacessivel-mapeamento-fase-cedente-20260915
Resumo: conexão SQL Server (PROD e HML) inacessível de novo, agora ao tentar mapear o grafo de FK da fase `cedente` (última fase pendente de mapeamento).
Pergunta-6: Com a dúvida anterior (Resposta-5) já implementada e commitada, tentei avançar para o próximo passo pendente da tarefa: mapear o grafo de FK real da fase `cedente` (28 tabelas, script `investigar-schema-cedente.cjs` que preparei nesta pasta, mesmo template usado nas fases anteriores) contra PROD. O comando `node investigar-schema-cedente.cjs` não retornou em mais de 6 minutos (bem acima do tempo normal de segundos). Repeti o mesmo diagnóstico já usado nos bloqueios anteriores desta tarefa (teste de TCP puro, `net.createConnection`, sem passar pelo driver/autenticação, script `.cjs` temporário nesta pasta, removido depois): timeout (~5s) tanto para `PROD_DB_HOST:PROD_DB_PORT` quanto para `HOMOLOG_DB_HOST:HOMOLOG_DB_PORT`. Mesmo padrão de infraestrutura já visto nos Ciclos 4/5 desta tarefa (não é dúvida de escopo/negócio) — preciso que confirme se a VPN/rede de acesso ao SQL Server está ativa nesta máquina para eu poder prosseguir com o mapeamento da fase `cedente` (última fase que falta mapear antes de implementar os comandos de leitura/INSERT/DELETE).
Resposta-6: Thiago confirmou (via Gerente, 2026-09-15): VPN reconectada agora, pode retentar o mapeamento da fase cedente (28 tabelas) que estava dando timeout de conexão SQL Server.

Status-historico-6: respondida (migrado acima)

Id-original-da-duvida-7: tres-dependencias-not-null-nao-resolviveis-fase-cedente-20260915
Resumo: 3 colunas NOT NULL na fase `cedente` (última fase do grafo) sem resolução automática possível — precisa decisão do Thiago pra cada uma.
Pergunta-7: Com a conexão restabelecida (VPN ok, ~150ms pros dois ambientes), mapeei o grafo de FK real das tabelas da fase `cedente` contra PROD (commit `765127e`, branch `cedente/clonar-cedente-completo-prod-hml`, ainda local). 25 das 28 tabelas citadas na tarefa foram totalmente resolvidas (3 — `MC_CED_GERENTE_FOCO_HIST`, `MC_CED_GERENTE_FOCO_LOG`, `MC_CED_FIRMAS_PODERES_REGRA_VALIDADE` — não existem de fato no schema, removidas da lista, não é decisão, só constatação). Sobraram 3 pontos bloqueantes, todos colunas NOT NULL sem resolução automática possível:

1. **`MC_CED_ATA_VOTACAO.idCedenteAta`** (NOT NULL) aponta para `MC_CED_ATA` — tabela que a própria tarefa já lista como fora de escopo ("Documentação e formalização inteira: ... MC_CED_ATA ..."). Essa tabela (`MC_CED_ATA_VOTACAO`) não estava na lista original da fase `cedente`, só foi citada por nome por você na Resposta-4 (junto com `MC_POC_COMITE_VOTACAO`/`MC_PORTAL_COMITE_VOTACAO`, pro tratamento de `idParticipante`/"votado e aprovado"). Ela existe de fato no schema e tem exatamente essa mesma estrutura (`idParticipante` NOT NULL sem FK física, `situacaoVoto`/`voto`), mas só pode ser inserida em HML se `MC_CED_ATA` (documento) também existir — o que contradiria a exclusão de documentação. Preciso de uma decisão: (1) incluir `MC_CED_ATA` no escopo só pra viabilizar essa FK (abrindo uma exceção pontual na exclusão de documentação)? (2) excluir `MC_CED_ATA_VOTACAO` inteiramente da cópia (tratá-la como parte da família "ata", fora de escopo, apesar de ter sido citada na Resposta-4)? (3) outro tratamento (ex.: criar um `MC_CED_ATA` "vazio"/mínimo em HML só como âncora, sem replicar o conteúdo do documento)?
2. **`MC_CED_CEDENTE_VINCULADO.idCedenteVinculado`** (NOT NULL) aponta pra OUTRO `MC_CED_CEDENTE` (um cedente relacionado/do mesmo grupo, não o cedente que está sendo clonado). A regra da tarefa é "um cedente por execução" (parametrizado por um único CNPJ/CPF via `--env`) — não decidi sozinho se: (1) a automação deveria localizar esse cedente vinculado em HML pelo CNPJ/CPF dele (mesma chave de match já usada pro cedente principal) e, se não existir lá, simplesmente pular essa linha de vínculo (não clona o vínculo, mas continua o resto normalmente)? (2) clonar em cascata o cedente vinculado também (contrariando "um por execução")? (3) sempre pular `MC_CED_CEDENTE_VINCULADO` por enquanto, tratando como fora de escopo prático até haver um caso real?
3. **`MC_CED_LOGIN.idLogin`** (NOT NULL) aponta pra uma tabela `MC_LOGIN` (fora do padrão `MC_CAD_*`, nome sugere login/autenticação do cedente no portal — não investiguei o conteúdo real das colunas de `MC_LOGIN` pra não arriscar expor dado sensível sem autorização). Preciso saber: (1) `MC_LOGIN` deve ser tratada como mais um catálogo fora do padrão `MC_CAD_*` (mesmo precedente de `MC_RAT_RATING_INDICADOR`, busca por chave natural em HML, cria se faltar)? (2) é dado sensível (credencial) e não deve ser clonado de jeito nenhum — nesse caso, `MC_CED_LOGIN` inteira fica fora de escopo? (3) outro tratamento?

O resto da fase `cedente` (25 tabelas, incluindo a âncora `MC_CED_CEDENTE`) já está mapeado e commitado, e não depende dessas 3 respostas — mas com essas pendências, o mapeamento do grafo de FK de toda a tarefa fica completo (todas as 4 fases: prospect, POC, comitê, cedente) e o próximo passo (escrever os comandos de leitura/INSERT/DELETE) pode começar a ser desenhado em paralelo, se preferir, tratando essas 3 tabelas como pendentes até a resposta chegar.
Resposta-7: Thiago respondeu via Gerente (2026-09-15), item por item:
1. `MC_CED_ATA_VOTACAO.idCedenteAta` → `MC_CED_ATA`: Thiago perguntou de volta "Consegue colocar para baixar o documento e adicioná-lo lá?" — antes de decidir entre âncora vazia ou exclusão, **investigue** se `MC_CED_ATA` guarda uma referência real ao documento (coluna de blob, path de arquivo, storage key, URL) e se é tecnicamente viável baixar o documento de PROD e inserir/anexá-lo em HML como parte da clonagem. Se for viável, implemente dessa forma (baixar o documento real e associá-lo em HML). Se não for viável (ex.: arquivo em storage externo inacessível daqui, ou não há coluna que referencie um arquivo de verdade), registre isso como nova dúvida e proponha explicitamente as duas alternativas restantes (âncora vazia em `MC_CED_ATA`, ou excluir `MC_CED_ATA_VOTACAO` inteira) para o Thiago escolher — não decida sozinho entre essas duas, só entre "baixar de verdade" vs "não é viável, escolha entre as alternativas". **Este item continua pendente** — ainda depende de investigação antes de ter uma resposta final.
   Complemento (2026-09-15, via Gerente): Thiago confirmou que existe um endpoint real pra baixar o documento: `GET https://beyond.grupomultiplica.com.br/mc-documento-ms/api/v1/download?idArquivo=<idArquivo>` (PROD, serviço `mc-documento-ms`), autenticado via header `Authorization: Bearer <token>`. Antes de implementar, investigue: (a) esta automação (módulo `cedente`, ou qualquer outro módulo deste projeto, ex. `keycloakUser`) já tem algum mecanismo de autenticação de serviço/service-account reutilizável contra APIs de PROD (client credentials via Keycloak, ou similar)? Se sim, reutilize esse mecanismo para obter um token válido para chamar esse endpoint — nunca hardcode um token de sessão pessoal/de navegador na automação, ele expira e é pessoal. (b) Se não existir nenhum mecanismo de auth de serviço reutilizável hoje, registre isso como um bloqueio novo e específico (não invente nem peça uma credencial pessoal) — não prossiga com um token pessoal copiado manualmente. (c) Se conseguir autenticar de forma legítima e reutilizável, baixe o documento de PROD (usando o `idArquivo` relevante de cada `MC_CED_ATA` da fase `cedente` sendo clonada) e implemente a inserção/anexação dele em HML como parte da clonagem — documentando o mecanismo de auth usado (só o nome do mecanismo/variável, nunca o valor) em `docs/documentacao.md`.
2. `MC_CED_CEDENTE_VINCULADO.idCedenteVinculado`: **decidido** — clonar em cascata. Se o cedente vinculado não existir em HML, acionar a clonagem dele também (mesmo fluxo desta tarefa, recursivamente). Ciente do risco de efeito cascata mencionado; prosseguir mesmo assim. Se a cascata for muito profunda/ampla ou encontrar um ciclo (cedente A vinculado a B vinculado a A), registrar nova dúvida antes de continuar.
3. `MC_CED_LOGIN.idLogin`: **decidido** — excluir. Não clonar `MC_CED_LOGIN` para HML, tratado como dado sensível/credencial. A FK fica sem resolução deliberadamente; como a coluna é NOT NULL e não pode ficar vazia, a tabela dependente inteira (`MC_CED_LOGIN`) deve ser excluída da clonagem, não só a coluna.

Itens 2 e 3 já implementados e commitados (commit `af709f8`, branch `cedente/clonar-cedente-completo-prod-hml`, ainda local — não pushado, tarefa não concluída): `MC_CED_CEDENTE_VINCULADO.idCedenteVinculado` com dependência tipo novo `cascata` (execução da cascata em si — buscar o vinculado em HML, disparar clonagem recursiva, detectar ciclo — ainda não implementada, só a declaração no grafo; a implementação real acontece quando os comandos de leitura/INSERT forem escritos, mesmo estágio dos demais tipos de dependência até aqui); `MC_CED_LOGIN` excluída inteira do escopo (`TABELAS_FORA_DE_ESCOPO`) e removida do mapeamento.

Id-original-da-duvida-8: mc-ced-ata-conteudo-inline-nao-e-documento-externo-20260916
Resumo: a premissa da Resposta-7/item 1 (existe um documento externo pra baixar via API) não se confirmou — `MC_CED_ATA` guarda o conteúdo da ata inline (texto/HTML), não como arquivo separado. Pede confirmação se ainda assim deve entrar no escopo.
Pergunta-8: Investiguei (2026-09-16, `INFORMATION_SCHEMA.COLUMNS` contra PROD, script `.cjs` temporário em `repo/`, removido antes do commit) a estrutura real de `MC_CED_ATA` para viabilizar o item 1 da Resposta-7 (`MC_CED_ATA_VOTACAO.idCedenteAta` → `MC_CED_ATA`). Resultado: `MC_CED_ATA` **não** tem uma referência real a um documento externo baixável — o conteúdo inteiro da ata vive na própria linha, em `textoAtaComite` (tipo texto, guarda HTML com pelo menos uma imagem embutida em base64, ex. assinatura/carimbo). A única coluna que de fato referencia um arquivo é `idArquivo` (nullable, sem FK física, aponta por convenção pra `MC_CAD_ARQUIVO`) — nula na maior parte das linhas da amostra. Ou seja, a pergunta original sobre "existe endpoint/mecanismo de auth reutilizável pra baixar o documento" (Resposta-7, complemento) não se aplica: não há nada para baixar via `mc-documento-ms`/Beyond neste caso — o "documento" já está embutido como dado de texto na própria tabela. Isso muda a natureza da decisão que pedi pra investigar: em vez de "é viável baixar o documento real", a pergunta agora é **"o Thiago quer mesmo copiar o conteúdo inteiro da ata (texto/HTML, potencialmente com imagem de assinatura embutida) para HML, como exceção pontual à exclusão explícita de documentação/formalização do escopo original da tarefa?"** Não decidi isso sozinho (regra 8 do `AGENTE.md` — inclusão de tabela fora do escopo original, e a própria tarefa lista `MC_CED_ATA` nominalmente entre as tabelas de documentação excluídas). Alternativas, na mesma linha das que eu já tinha proposto antes de saber que não havia documento externo: (1) incluir `MC_CED_ATA` com o conteúdo completo (`textoAtaComite` copiado como está, `idArquivo` não resolvido/fica null, mesmo precedente de `idArquivoLogo`) só para viabilizar `MC_CED_ATA_VOTACAO`, aplicando também o valor fixo de "votado" (`situacaoVotacao: 'FINALIZADA'`, mesmo padrão da Resposta-5) — e então implementar `MC_CED_ATA_VOTACAO` com `idParticipante` como participante fixo (mesmo padrão já usado nas votações de comitê); (2) manter `MC_CED_ATA` fora do escopo (como a tarefa original já definia) e, por consequência, `MC_CED_ATA_VOTACAO` também fica fora (não é possível inserir sem a ata-pai); (3) outro tratamento. O restante da fase `cedente` (25 tabelas + os itens 2/3 já resolvidos acima) não depende desta resposta — só `MC_CED_ATA`/`MC_CED_ATA_VOTACAO` ficam pendentes.
Resposta-8: Thiago escolheu a opção (1) (2026-09-16): incluir `MC_CED_ATA` com o conteúdo completo
(copiar `textoAtaComite` como está, incluindo a imagem embutida em base64 se houver; `idArquivo`
segue não resolvido/null, mesmo precedente já usado em outras colunas de arquivo opcional), aplicar
o mesmo valor fixo de "votado" (`situacaoVotacao: 'FINALIZADA'`, padrão da Resposta-5), e então
implementar `MC_CED_ATA_VOTACAO` com `idParticipante` como participante fixo (mesmo padrão já usado
nas votações de comitê, Resposta-4). Isso é uma exceção pontual à exclusão de documentação do
escopo original, só para viabilizar `MC_CED_ATA_VOTACAO` — não abre precedente para outras tabelas
de documentação/formalização ainda fora de escopo.

Id-original-da-duvida-9: colunas-auditoria-not-null-insert-direto-sql-catalogo-20260916
Resumo: todo INSERT direto via SQL em HML (não pela API mc-cadastro-ms) falha por colunas de auditoria NOT NULL que a API preenche sozinha mas o SQL direto não — decisão de qual valor fixo usar para "usuário" afeta todas as tabelas do domínio cedente, não só catálogo.
Pergunta-9: Com o grafo de FK das 4 fases completo (Ciclo 11) e a etapa de leitura/estratégia já testada (Ciclo 12), este ciclo implementou o resolvedor genérico de dependência de catálogo (`cy.resolverIdCatalogoEmHml`, `commands/catalogoCedente.js`) — busca em HML por chave natural (`METADADOS_CATALOGO_CEDENTE`) e cria uma cópia da linha de PROD (minus colunas de auditoria) se não existir. Testado fim a fim contra PROD/HML reais: o caminho de "já existe" funciona (`MC_CAD_SITUACAO` id 1 PROD "ATIVA" → resolvido para id 1 já existente em HML). O caminho de "criar" **falhou**: `INSERT INTO MC_CAD_SITUACAO (...) OUTPUT INSERTED.id VALUES (...)` (id 43 PROD, "MAJORADO", ausente em HML) deu erro real do SQL Server — `Cannot insert the value NULL into column 'usuarioUltimaAlteracao'... column does not allow nulls`. Investiguei (`INFORMATION_SCHEMA.COLUMNS` contra HML, script `.cjs` temporário em `repo/`, removido antes do commit): as 4 colunas de auditoria (`dataCadastro`, `dataUltimaAlteracao`, `usuarioCadastro`, `usuarioUltimaAlteracao`) são **NOT NULL em 38 das 38 tabelas de catálogo verificadas** (praticamente universal no schema, não uma exceção pontual de `MC_CAD_SITUACAO`) — `usuarioCadastro`/`usuarioUltimaAlteracao` são `varchar` (não FK/id), guardando o username de quem criou/alterou o registro (amostra real em `MC_CAD_SITUACAO`: `"henrique"`, `"tiago.roque"`, e também o valor literal `"sistema"` em pelo menos uma linha, aparentemente já usado hoje para registros gerados automaticamente). Isso não é um problema desta tabela específica — é estrutural: todos os outros domínios do repo (Produtos/Esteiras/Vínculos/Grupos e Permissões) criam registros em HML via **API REST** (`mc-cadastro-ms`), que preenche essas colunas no servidor; o domínio `cedente` inteiro usa SQL direto (`dbClient.cjs`) por não haver endpoint mapeado para ~122 tabelas, então nada preenche essas colunas automaticamente — vou precisar fornecer valores explícitos em **todo** INSERT que este domínio fizer (catálogo agora, e as tabelas estruturais quando essa etapa for implementada), não só neste caso. Preciso de uma decisão (regra 8 do `AGENTE.md` — "qual padrão do projeto seguir", decisão que se propaga para o resto da tarefa, não decido sozinho): (1) `dataCadastro`/`dataUltimaAlteracao` = timestamp do momento da inserção (não há outro valor sensato — sem ambiguidade aqui, só confirmando) e `usuarioCadastro`/`usuarioUltimaAlteracao` = string fixa `'sistema'` (reaproveita o valor literal que já existe como precedente real nos dados de `MC_CAD_SITUACAO`)? (2) uma string fixa diferente, mais identificável como vindo desta automação (ex. `'automacao-cedente'`, sem precedente nos dados reais, mas mais fácil de rastrear numa auditoria futura "quem criou isso")? (3) usar o nome do próprio Thiago (mesmo valor já usado como responsável fixo da votação, `NOME_ANALISTA_RESPONSAVEL_CLONAGEM_CEDENTE`, ou seu login de rede), já que ele é o responsável formal por esta automação? (4) outro valor? Sem essa decisão, a etapa de "criar" do resolvedor de catálogo (e, mais adiante, todo INSERT estrutural) fica implementada mas não pode ser exercitada de verdade em nenhuma tabela nova — o caminho de "já existe" (achar por chave natural) não é afetado e continua funcionando. Nenhuma linha ficou de fato criada em HML pela tentativa que falhou (o INSERT inteiro não foi efetivado pelo SQL Server ao dar erro de NOT NULL).
Resposta-9: Thiago confirmou (2026-09-16): opção (1) — `dataCadastro`/`dataUltimaAlteracao` =
timestamp do momento da inserção, e `usuarioCadastro`/`usuarioUltimaAlteracao` = string fixa
`'sistema'` (mesmo valor já usado como precedente real nos dados de `MC_CAD_SITUACAO`). Aplicar
esse padrão em todo INSERT que o domínio `cedente` fizer via SQL direto (catálogo agora, e as
tabelas estruturais quando essa etapa for implementada) — não é uma decisão pontual só para
`MC_CAD_SITUACAO`.
