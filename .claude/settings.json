{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "git fetch origin main --quiet 2>$null; $branch = (git rev-parse --abbrev-ref HEAD 2>$null); $dirty = (git status --porcelain 2>$null); if ($branch -eq 'main' -and [string]::IsNullOrEmpty($dirty)) { git pull origin main --quiet 2>$null; if ($LASTEXITCODE -eq 0) { $msg = 'main atualizado automaticamente via git pull (raiz C:\\Multiplica\\claudeAgents, compartilhada por todos os Supervisores/agentes).' } else { $msg = 'Nao foi possivel dar git pull em main (possivel conflito) - verifique manualmente antes de continuar.' } } else { if ([string]::IsNullOrEmpty($dirty)) { $pend = 'nao' } else { $pend = 'sim' }; $msg = 'Pull automatico de main nao executado (branch atual: ' + $branch + ', mudancas pendentes: ' + $pend + '). Rode git pull origin main manualmente se precisar do estado mais recente.' }; Write-Output ('{\"systemMessage\": \"' + $msg + '\"}')",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
